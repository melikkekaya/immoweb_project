import requests, re, json, pandas, threading
from bs4 import BeautifulSoup



def get_all_urls(root_url):
    """
    scraps all pages of houses and aparments, 
    and returns a list of urls of properties
    """
    pass

def scrap_info(property_url):
    """
    gets info from property urls as json for each, 
    writes those infos into a csv and returns one csv with all info of all urls.
    """
    pass
    



